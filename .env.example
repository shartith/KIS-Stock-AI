# ==========================================
# KIS Stock AI - Environment Configuration Template
# ==========================================
# 사용법:
# 1. 이 파일을 .env로 복사합니다: cp .env.example .env
# 2. .env 파일을 열어 실제 값을 입력합니다.
# ==========================================

# ── 🏦 한국투자증권 (KIS) API ──
# https://apiportal.koreainvestment.com/
KIS_APP_KEY=your_app_key_here
KIS_SECRET_KEY=your_secret_key_here
KIS_ACCT_STOCK=1234567801  # 계좌번호 (10자리)

# ── 🚀 Antigravity / Google AI ──
# Google OAuth 2.0 (Antigravity IDE 연동)
GOOGLE_OAUTH_CLIENT_ID=your_google_client_id
GOOGLE_OAUTH_CLIENT_SECRET=your_google_client_secret

# Legacy API Key (옵션, Fallback용)
ANTIGRAVITY_API_KEY=your_google_ai_studio_key
ANTIGRAVITY_MODEL=gemini-2.0-flash

# ── 💬 알림 (Notification) ──
# Discord Webhook URL (설정 > 연동 > 웹후크 만들기)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# ── 🤖 AI 설정 (Advanced) ──
AI_MODE=local  # local 또는 antigravity
LOCAL_LLM_URL=http://127.0.0.1:8002
LOCAL_LLM_MODEL=bitnet-3b

# ── 📊 거래 설정 ──
ALLOW_LEVERAGE=0      # 1=허용, 0=차단
ENABLE_AUTO_SCAN=1    # 1=활성, 0=중지
ENABLE_AUTO_BUY=0     # 1=자동매수, 0=모니터링만
ENABLE_AUTO_SELL=0    # 1=자동매도, 0=수동매도
