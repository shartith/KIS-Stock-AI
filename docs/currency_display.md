# 통화 표시 시스템 (Currency Display)

주식 가격을 해당 시장의 네이티브 통화로 표시합니다.

---

## 1. 통화 매핑

### JavaScript (trading.html, portfolio.html)

```javascript
const CURRENCY_MAP = {
    'US': '$',   'NASD': '$',  'NYSE': '$',   'AMEX': '$',
    'JP': '¥',   'TKSE': '¥',
    'HK': 'HK$', 'SEHK': 'HK$',
    'CN': '¥',   'SHAA': '¥',  'SZAA': '¥',
    'KR': '₩',   'KRX': '₩',   'KOSPI': '₩',  'KOSDAQ': '₩',
    'UK': '£',   'LNSE': '£',
    'VN': '₫',   'HASE': '₫',  'VNSE': '₫',
};
```

> **참고**: 시장 코드와 거래소 코드 모두 매핑합니다 (예: `JP` = 시장, `TKSE` = 거래소)

---

## 2. 가격 포매팅 함수

```javascript
function fmtPrice(price, market, decimals) {
    const ccy = getCcy(market);  // 통화 심볼 조회
    const d = decimals !== undefined ? decimals :
        (market === 'JP' || market === 'TKSE') ? 0 :          // 엔화: 소수점 없음
        (market === 'KR' || market === 'KRX' || ...) ? 0 :    // 원화: 소수점 없음
        (market === 'CN' || market === 'SHAA' || ...) ? 2 : 2; // 기타: 소수점 2자리
    return ccy + (price || 0).toLocaleString(undefined, {
        minimumFractionDigits: d,
        maximumFractionDigits: d
    });
}
```

### 소수점 규칙

| 시장 | 통화 | 소수점 | 예시 |
|---|---|---|---|
| 🇯🇵 일본 | ¥ | 0 | ¥6,262 |
| 🇰🇷 한국 | ₩ | 0 | ₩52,300 |
| 🇺🇸 미국 | $ | 2 | $173.45 |
| 🇭🇰 홍콩 | HK$ | 2 | HK$76.10 |
| 🇨🇳 중국 | ¥ | 2 | ¥35.20 |
| 🇬🇧 영국 | £ | 2 | £45.80 |
| 🇻🇳 베트남 | ₫ | 0 | ₫25,000 |

---

## 3. 적용 위치

### trading.html

| 위치 | 필드 |
|---|---|
| Analysis Results 테이블 | 현재가 |
| Buy Candidate 카드 | 실시간 가격, AI 매수가, S/L, T/P, 체결가 |

### portfolio.html

| 위치 | 필드 |
|---|---|
| 해외보유종목 테이블 | 평균매입가, 현재가, AI 매도가, S/L, 손익분기, 수수료, 순이익 |
| Trade Log 테이블 | 가격, 수수료, 순이익 |

---

## 4. 사용법

```javascript
// 기본 사용
fmtPrice(6262, 'JP')          // → "¥6,262"
fmtPrice(173.45, 'US')        // → "$173.45"
fmtPrice(52300, 'KR')         // → "₩52,300"

// 소수점 직접 지정
fmtPrice(0.05, 'US', 4)       // → "$0.0500"

// market 또는 exchange 코드 모두 가능
fmtPrice(6262, 'TKSE')        // → "¥6,262"
fmtPrice(76.10, 'SEHK')       // → "HK$76.10"
```
